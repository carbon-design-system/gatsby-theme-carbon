{
  "name": "example",
  "private": true,
  "version": "0.5.2",
  "license": "Apache 2.0",
  "scripts": {
    "develop": "gatsby develop",
    "develop:clean": "rm -rf ./.cache && yarn develop",
    "build": "gatsby build",
    "build:prefix": "gatsby build --prefix-paths",
    "build:debug": "node --inspect ../../node_modules/.bin/gatsby build --prefix-paths",
    "serve": "gatsby serve"
  },
  "dependencies": {
    "gatsby": "^2.4.2",
    "gatsby-theme-carbon": "^0.5.2",
    "react": "^16.8.6",
    "react-dom": "^16.8.6"
  }
}
